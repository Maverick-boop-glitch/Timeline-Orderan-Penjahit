{
  "name": "jahitan-app",
  "version": "1.0.0",
  "private": true,
  "description": "Sistem laporan dan estimasi timeline order penjahit — Cloudflare Worker + D1",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute jahitan-db --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
